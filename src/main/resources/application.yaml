server:
  port: 8080
  # --- QUAN TRỌNG: BẬT HIỂN THỊ LỖI CHI TIẾT ---
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todolist?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    # Mật khẩu chứa ký tự đặc biệt nên để trong dấu nháy đơn
    password: '@Acctonvn12345'
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # --- THÊM ĐOẠN NÀY ĐỂ FIX LỖI NGÀY THÁNG (ÉP KIỂU CÓ CHỮ T) ---
  jackson:
    date-format: yyyy-MM-dd'T'HH:mm:ss
    time-zone: Asia/Ho_Chi_Minh
    serialization:
      write-dates-as-timestamps: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: laichibaomessi56@gmail.com
    # Lưu ý: Đây phải là Mật khẩu ứng dụng 16 ký tự
    password: MAT_KHAU_UNG_DUNG_CUA_BAN
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
      resolve-lazily: true

# Cấu hình đường dẫn lưu file (Tự động lấy thư mục User)
app:
  upload:
    dir: ${user.home}/inventory_uploads

# Cấu hình JWT
security:
  jwt:
    secret: sdjfskdjhkjfhsdkjhfksdhkjfshdkjhfksdhkfhskdhkfshdkjfhksdhfkjhsdfhskdhkfshdkshkdhksdhfsdkj
    expiration: 86400000

# Cấu hình CORS (Biến này để chơi thôi, cấu hình thật nằm trong SecurityConfig.java)
inventory:
  cors:
    allowed-origins: http://localhost:5173

# Cấu hình Log
logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    com.example.inventory: DEBUG
  file:
    name: logs/inventory.log